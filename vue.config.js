module.exports = {
    css: {
      loaderOptions: {
        scss: {
        //   additionalData: `@import "~@/custom.scss";`
        }
        }
      }
    }

    // запуск serve:
    // npm run build
    // serve -s dist

//  Команда сборки: npm run build или yarn build
// Каталог публикации: serve -s dist
